# تعليمات إعداد وتشغيل مشروع SIOMS

## المتطلبات الأساسية

- Node.js 18+ و npm أو pnpm
- الباك اند يعمل على `http://localhost:5000`

## خطوات التثبيت والتشغيل

### 1. تثبيت المكتبات

```bash
cd sioms-frontend
npm install
# أو
pnpm install
```

### 2. إعداد متغيرات البيئة

تأكد من أن ملف `.env.local` يحتوي على:
```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

### 3. تشغيل الباك اند

```bash
cd sioms-backend
npm install
npm start
```

يجب أن ترى رسالة:
```
🚀 SIOMS Backend → http://localhost:5000
📋 Health check → http://localhost:5000/api/health
```

### 4. تشغيل الفرونت اند

في نافذة terminal جديدة:
```bash
cd sioms-frontend
npm run dev
```

سيفتح المتصفح على `http://localhost:3000`

## اختبار الاتصال

### تحقق من الـ API Health:
```bash
curl http://localhost:5000/api/health
```

يجب أن تحصل على:
```json
{
  "status": "OK",
  "message": "SIOMS API running",
  "timestamp": "2026-02-20T10:00:00.000Z"
}
```

## الميزات المطبقة

✅ **إزالة Mock Data** - جميع البيانات من الباك اند الحقيقي
✅ **Validation شامل** - على جميع النماذج والمدخلات
✅ **Export إلى CSV** - للحضور والرواتب والمخزون
✅ **API Integration** - ربط كامل مع الباك اند

## المشاكل الشائعة وحلولها

### المشكلة: "Failed to connect to API"
**الحل:** تأكد من أن الباك اند يعمل على المنفذ 5000

### المشكلة: "CORS Error"
**الحل:** تأكد من أن الباك اند يسمح بـ CORS من localhost:3000

### المشكلة: "Invalid token"
**الحل:** قم بتسجيل الدخول مرة أخرى

## الملفات المهمة

- `src/services/apiClient.ts` - إعدادات الـ API
- `src/components/modules/` - مكونات الوحدات الرئيسية
- `.env.local` - متغيرات البيئة

## التطوير والاختبار

### اختبار Validation:
1. اذهب إلى صفحة HR
2. حاول إضافة موظف بدون بريد إلكتروني
3. يجب أن تظهر رسالة خطأ واضحة

### اختبار Export:
1. اذهب إلى صفحة Attendance
2. انقر على زر Export
3. يجب أن ينزل ملف CSV

## الدعم

للمزيد من المعلومات، راجع `UPDATES_DOCUMENTATION.md`
